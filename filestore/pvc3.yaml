apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: fileserver-claim3
spec:
    accessModes:
    - ReadWriteMany
    storageClassName: ""
    volumeName: fileserver2
    resources:
        requests:
            storage: 50G
